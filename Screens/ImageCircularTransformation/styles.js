import { StyleSheet } from "react-native";
export const style = StyleSheet.create({
  mainView: {
    flex: 1,
    backgroundColor: "#fff",
    justifyContent: "center",
    alignItems: "center"
  },
  imageStyele: {
    height: 100,
    width: 127
  }
});
